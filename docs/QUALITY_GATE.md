# Quality Gate

Sprint 5 introduces additional release checks.

Checks:
- Plugin structure validation
- Plugin header validation
- Autoload smoke test entrypoint

Goal:
A release must be tested as an installable artifact, not only as source code.
